extends layout

block content
  .container(style="margin-top: 10%")
    .row.text-center
      .col-xs-12.col-sm-6.col-sm-offset-3.col-md-4.col-md-offset-4
        h1.bitelio(style="font-size: 60px; color: #ccc;") bitelio
        p.lead Error #{error.status}
        p(style='color: #777')= message
    .row.text-left
      .col-xs-12
        pre #{error.stack}

